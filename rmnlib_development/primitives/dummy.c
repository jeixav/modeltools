void bind_to_localport(){
  exit(1);
}

void accept_from_sock(){
  exit(1);
}

void write_stream(){
  exit(1);
}

void read_stream(){
  exit(1);
}

void check_swap_records(){
  exit(1);
}

void tracebck_(){
  exit(1);
}
